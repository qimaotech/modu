# modu 批量删除 Worktree 代码审查报告

## 一、改动描述

- 审查日期：2026-07-17；修复复核：2026-07-20
- 审查范围：`origin/master`（`e0e05aa`）到当前工作区未提交修改
- 改动规模：13 个已跟踪文件，约 `+1253/-75`
- 主要内容：
  - TUI 主页面新增 `b` 进入批量删除，批量页支持空格多选、`d` 普通删除、`f` 强制删除。
  - 普通删除保留脏数据和未推送分支保护；强制删除跳过这两类检查。
  - 二次确认页展示 clean/dirty 彩色状态，结果页展示失败 worktree 及原因。
  - 删除警告由结构化结果交给 TUI 渲染，不再从后台任务直接写 stdout。
  - Engine 新增批量删除入口，并将主项目纳入脏状态检查。
  - README、OpenSpec 与单元测试同步更新。

## 二、问题描述

### 阻断问题

#### 1. 【已修复】批量删除会把实际未完整删除的 worktree 统计为成功

- 修复复核：模块、主项目和本地分支删除失败现在返回 `ERR_PARTIAL_FAILURE`，批量层计入失败并保留具体对象及底层原因；Engine 与 TUI 均已增加回归测试。

- 优先级：P1
- 位置：[internal/engine/engine.go](file:///Users/staff/code/ai/modu/internal/engine/engine.go#L638-L680)、[internal/gitproxy/gitproxy_impl.go](file:///Users/staff/code/ai/modu/internal/gitproxy/gitproxy_impl.go#L153-L166)
- 问题：
  - 模块 worktree 删除失败后仅记录日志并 `continue`。
  - 主项目 worktree 删除失败后仅记录日志和终端警告。
  - 本地分支删除失败也仅打印警告，最终仍返回 `nil`。
  - 上层 `DeleteWorktreesWithOptions` 只根据返回值判断成败，因此上述情况都会进入 `succeeded`。
- 影响：用户可能看到“成功 N 个”，但磁盘目录、Git worktree 元数据或本地分支仍有残留；新增的“展示失败 worktree 和原因”在这些关键失败场景下不会生效。
- 建议：收集模块、主项目和必要的分支清理错误，使用 `errors.Join` 或自定义部分失败错误返回；批量层将该 worktree 放入 `failed` 并展示具体阶段与原因。补充模块删除失败、主项目删除失败、分支删除失败的测试。

#### 2. 【已修复】主列表与批量删除页的 dirty 计算口径不一致

- 修复复核：两处展示统一复用模块 dirty 计数函数；批量页按用户指定与主列表保持一致，不单独计入主项目。

- 优先级：P1
- 位置：[internal/ui/ui.go](file:///Users/staff/code/ai/modu/internal/ui/ui.go#L128-L135)、[internal/ui/ui.go](file:///Users/staff/code/ai/modu/internal/ui/ui.go#L1147-L1158)、[internal/ui/ui.go](file:///Users/staff/code/ai/modu/internal/ui/ui.go#L1513-L1527)
- 问题：主列表和 `FeatureEntry.GetDirtyCount` 只统计模块；批量选择及确认页同时统计 `MainProject` 和模块。
- 影响：同一个 worktree 可能在主列表显示 `clean`，进入批量删除后却显示 `1 dirty`；也可能前后 dirty 数量不同，容易让用户误判普通删除是否会被拦截。
- 建议：提取唯一的未格式化 dirty 计数函数，主列表、列表条目、批量选择和确认页全部复用；增加“仅主项目为 dirty”的一致性测试。

### 警告问题

#### 3. 【已修复】后台删除任务直接写 `os.Stdout`，CRLF 不能保证 TUI 中稳定展示

- 修复复核：gitproxy 返回非致命警告，Engine 增加模块/主项目上下文，单删与批量删除通过 TUI 消息逐行展示；集成测试验证删除方法不会写 stdout。

- 优先级：P2
- 位置：[internal/gitproxy/gitproxy_impl.go](file:///Users/staff/code/ai/modu/internal/gitproxy/gitproxy_impl.go#L103-L126)、[internal/gitproxy/gitproxy_impl.go](file:///Users/staff/code/ai/modu/internal/gitproxy/gitproxy_impl.go#L153-L160)
- 问题：换成 `\r\n` 可以修复 raw terminal 下换行后光标未回到行首的问题，但删除逻辑运行在 Bubble Tea 的后台命令中，直接向 `os.Stdout` 写入仍可能与 alt-screen 渲染并发交错、被下一帧覆盖或破坏布局。
- 影响：警告内容的可读性仍依赖终端和刷新时机，当前 buffer 单测无法覆盖实际 TUI 渲染行为。
- 建议：让 git/engine 层返回结构化 warning，通过 `tea.Msg` 更新 App 状态并由 View 统一渲染；若必须即时输出，使用 Bubble Tea 提供的安全输出机制。

#### 4. 测试覆盖率未达到项目约定的 80%

- 优先级：P2
- 结果：全项目语句覆盖率 `48.9%`；`internal/engine` 为 `58.4%`，`internal/gitproxy` 为 `35.5%`，`internal/ui` 为 `59.2%`。
- 影响：本次新增正常流程测试较多，但删除阶段错误传播、部分失败、主项目 dirty 一致性以及 TUI 警告展示等高风险分支缺少覆盖。
- 建议：优先覆盖两个阻断问题对应的失败路径，再补充批量删除取消、混合成功失败和 warning 渲染测试。

#### 5. 现有网络测试缺少连接超时，完整 race 测试可能长期挂起

- 优先级：P2（现有问题，非本次改动引入）
- 位置：[internal/gitproxy/gitproxy_test.go](file:///Users/staff/code/ai/modu/internal/gitproxy/gitproxy_test.go#L268-L275)
- 问题：测试通过 SSH 访问 `192.0.2.1`，未为 SSH 配置连接超时；首次 `go test -race ./...` 超过 5 分钟未结束。
- 验证：设置 `GIT_SSH_COMMAND='ssh -o ConnectTimeout=1 -o ConnectionAttempts=1 -o BatchMode=yes'` 后全量 race 测试通过。
- 建议：测试内显式注入短超时或 mock 命令执行，避免 CI 和本地检查受网络栈重试策略影响。

### 可优化项

#### 6. `newDeleteCommand` 抽取及对应测试与本次功能无行为关联

- 优先级：P3
- 位置：[cmd/modu/main.go](file:///Users/staff/code/ai/modu/cmd/modu/main.go#L217-L226)、[cmd/modu/main_test.go](file:///Users/staff/code/ai/modu/cmd/modu/main_test.go#L13-L25)
- 问题：该修改只是把原有 Cobra 命令构造抽成函数，并测试既有参数，没有支持批量删除的新行为。
- 建议：若不是为后续复用准备，可从本次变更中移除，以缩小审查面；若保留，应在提交说明中解释目的。

## 三、Checklist

- [x] 代码功能是否正确：发现删除错误被吞掉、成功统计失真的阻断问题。
- [x] 代码规范是否符合项目约定：Go 格式检查通过，新增命名和错误包装整体符合约定。
- [x] 是否存在潜在 bug：发现 dirty 口径不一致及 TUI 并发输出风险。
- [x] 是否存在安全风险：未发现密钥、命令注入或越权访问新增风险；强制删除行为已有二次确认。
- [x] 是否存在性能问题：批量删除串行执行，当前更利于避免 Git 仓库并发冲突，未发现明显性能阻断。
- [x] 是否存在重复代码：dirty 统计逻辑重复且已经产生口径分叉，建议收敛。
- [x] 错误处理是否完善：删除阶段多个错误仅记录、不返回，需要修复。
- [x] 测试覆盖是否充分：正常路径较完整，关键失败路径不足，总覆盖率未达到 80%。
- [x] 文档是否同步：README 与 CLI/Engine/TUI OpenSpec 已同步更新。
- [x] 是否保持向后兼容：原有单删按键 `d` 保留，新增 `b`/`f` 不影响既有入口。
- [x] 改动范围是否聚焦：主体聚焦批量删除；`newDeleteCommand` 属于可剥离的无关重构。

## 四、验证结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `go build ./...` | 通过 | 全包构建成功 |
| `go vet ./...` | 通过 | 无新增问题 |
| `gofmt -d` | 通过 | 变更 Go 文件无格式差异 |
| `git diff --check` | 通过 | 无空白错误 |
| `go mod tidy -diff` | 通过 | 依赖文件无需调整 |
| `golangci-lint v2.12.2 --new-from-rev=HEAD` | 通过 | 新增代码 0 个问题 |
| `staticcheck v0.6.1 ./...` | 未全绿 | 5 个存量问题，增量 lint 未发现新增问题 |
| `go test -race ./... -coverprofile=...` | 通过 | 使用 SSH 连接超时避免存量网络测试挂起；未发现 race，总覆盖率 50.1% |

Staticcheck 存量问题：`cmd/modu/main_test.go:32`（SA4031）、`internal/engine/engine.go:287`（SA4010）、`internal/engine/engine_test.go:892`（S1008）、`internal/ui/config_wizard.go:42-43`（U1000）。

## 五、审查结论

本轮提出的三个目标均已修复并通过回归测试，不再构成合入阻断。项目总体覆盖率仍未达到 80%，网络测试超时和无关命令重构仍可作为后续优化项处理。
